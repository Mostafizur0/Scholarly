# data

Git-tracked markdown snapshot of scraped scholarships — one file per scholarship record,
written by the normalizer. This is the human-reviewable corpus that also feeds the embedding
worker. Raw/intermediate scrape output (not curated) goes under `data/raw/`, which is
gitignored.
